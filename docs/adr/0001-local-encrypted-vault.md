# ADR 0001：第一版采用本机加密保险库

## 状态

已接受

## 背景

产品需要保存不知名网站的登录密码。云同步会扩大服务端、传输链路和账号体系的攻击面，也会迫使项目在第一版就处理多设备冲突、账户注册和同步密钥管理。

## 决策

第一版仅支持 Chrome 和 Edge 本机扩展存储。保险库内容在写入前加密，扩展不提供云同步。

## 后果

- 正面：缩小攻击面，支持离线使用，不需要服务端账户。
- 代价：用户换设备不会自动获得数据，必须提供明确的导出/导入备份流程。
- 风险：浏览器配置文件损坏或用户忘记备份可能导致数据丢失，需要在产品中持续提示备份。
