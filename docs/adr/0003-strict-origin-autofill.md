# ADR 0003：自动填充采用严格 origin 匹配并需用户触发

## 状态

已接受

## 背景

自动填充如果只按域名后缀或页面字段推断，可能把密码发送到仿冒子域、同名域或恶意嵌入页面。自动提交还会让用户难以及时发现填充目标错误。

## 决策

第一版仅在当前页面完整 origin（协议、主机和端口）与条目记录一致时允许填充，并要求用户点击扩展或页面控件主动触发。填充后不自动提交表单。

## 后果

- 正面：降低凭据误填和钓鱼风险，行为可被用户感知。
- 代价：同一服务的不同登录 origin 需要分别保存条目；部分网站需要用户多一步操作。
