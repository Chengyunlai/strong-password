# Edge 1.0.1 发布说明

版本：1.0.1

- 移除常驻全站 content script；填充改为用户点击后请求对应 HTTPS origin 权限，再通过 `scripting` 临时执行。
- 使用 Edge/Chrome 侧边栏承载主界面，点击网页其他位置不会关闭扩展。
- 自动识别当前活动标签页 origin；首次填充某个网站时按站点请求访问权限。
- 解锁状态在本次浏览器会话内保持，闲置 15 分钟、浏览器重启或主动锁定后自动失效。
- 首次创建保险库并确认恢复密钥后直接进入已解锁保险库，不重复输入主密码。
- 填充逻辑兼容受控表单，并支持注册页面的密码确认输入框。

## 1.0.1 修复

- 修复商店安装版侧边栏持续显示“正在识别当前网站”的问题。
- 持久侧边栏改用 `tabs` 权限读取当前活动标签页 URL；只提取 origin 用于匹配，不读取页面正文或上传浏览记录。

Edge 商店资料应说明：数据只保存在本地，不上传密码；`tabs` 用于读取当前活动标签页 URL，填充权限按用户操作、按站点授予；隐私政策使用公开 HTTPS URL。
