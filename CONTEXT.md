# 项目上下文

## 术语表

### 浏览器范围

本项目第一版支持 Chrome 和 Microsoft Edge（均为 Chromium 内核）。Firefox、Safari 及移动端浏览器不在第一版范围内。

### 密码条目

保险库中代表一个网站或服务登录凭据的记录，至少包含站点标识、用户名和密码，并可附带备注。

### 保险库

用户解锁后可查看和管理密码条目的受保护集合。

### 本机加密保险库

保险库数据仅保存在用户设备的浏览器扩展存储中，离开设备不会自动复制到服务端或其他设备。

### 重置主密码

不等同于恢复保险库。没有旧主密码或有效恢复材料时，重置只能重新初始化保险库，原有加密数据不可读取。

### 恢复密钥

创建保险库时生成、仅向用户展示一次并由用户离线保管的恢复材料。它用于在忘记主密码时重新设置主密码，不通过安全问题或服务端找回。

### 超强密码

默认由密码学安全随机数生成的 24 位密码，包含大小写字母、数字和符号；用户可以按目标网站的兼容性调整长度和字符集。

### 严格自动填充

只有当当前页面的完整 origin 与密码条目的站点 origin 一致，并且用户主动触发填充操作时，扩展才会填入凭据；扩展不会自动提交表单。

### Beta 发布

公开版本标记为 Beta。扩展在用户点击操作后才临时访问当前标签页，不常驻读取所有网站；关闭弹窗会立即丢弃解锁密钥，弹窗保持打开时 5 分钟无操作后自动清除；复制的密码会在 30 秒后尽力从剪贴板清除。
