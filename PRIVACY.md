# Strong Password Vault 隐私政策

最后更新：2026-08-22

Strong Password Vault 是一个本地优先的浏览器扩展。

## 数据处理与存储

扩展会在用户设备本地处理并以加密形式保存用户主动添加的站点 origin、用户名和密码；用户名可能是电子邮件地址，站点 origin 可反映用户选择保存的登录网站。主密码和恢复密钥不会以明文保存。

扩展不将密码、主密码、恢复密钥、用户名、站点 origin 或保险库内容上传给开发者、第三方或远程服务器；不出售或共享这些数据。

## 页面访问

`tabs` 权限在技术上可以访问所有标签页的 URL 和标题元数据；持久侧边栏不能依赖短暂的 `activeTab` 权限，因此需要该权限。当前实现只在侧边栏开启期间，于活动标签页切换或加载完成时查询该活动页 URL，并提取 origin 用于网站匹配；不会读取其他标签页、页面正文，也不会持久化或上传标签页元数据。只有用户主动点击“填充”时，才会请求该 HTTPS 网站的访问权限并访问登录输入框。访问权限按网站单独授予。扩展不会自动提交表单，也不会将页面内容发送到远程服务器。复制到剪贴板的密码会尝试在 30 秒后清理，但浏览器权限或关闭弹窗可能使清理无法执行。

## 数据备份

用户可主动导出加密备份文件。备份文件由用户自行保存和管理；扩展不会上传或同步该文件。

## 联系方式

如需反馈安全问题或隐私问题，请通过 GitHub Issues 联系维护者：
https://github.com/Chengyunlai/strong-password/issues
