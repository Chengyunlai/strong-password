# Edge Add-ons 商店资料

## 名称

强密码库

## 简短描述

自动生成强密码，并在本机加密保险库中安全保存和主动填充。

## 详细描述

强密码库帮助你为不熟悉的网站生成独立密码，并在本机保存凭据：

- 默认生成 24 位密码，包含大小写字母、数字和符号
- 自动识别当前 HTTPS 网站，已保存条目会直接匹配
- 使用主密码保护本机加密保险库
- 提供一次性恢复密钥，不使用安全问题
- 仅在用户主动填充且完整 origin 匹配时填入凭据
- 首次填充某个网站时单独请求该网站的访问权限
- 支持加密备份导出与导入
- 使用浏览器侧边栏，点击网页其他位置不会关闭
- 解锁状态在当前浏览器会话内保持，闲置 15 分钟后自动锁定

数据只保存在用户设备本地，不上传密码，不提供云同步。

这是本地优先的安全工具。当前版本尚未完成独立安全审计，建议先用于低风险网站；不要保存银行、交易所或主邮箱等关键凭据。剪贴板清理属于尽力而为，不能替代手动清空。

## 隐私政策 URL

https://github.com/Chengyunlai/strong-password/blob/master/PRIVACY.md

## 权限说明

- `storage`：保存加密后的本机保险库记录。
- `storage.session`：在当前浏览器会话内保存解锁所需的临时密钥材料；浏览器重启后清除。
- `activeTab`：用户点击扩展后临时读取当前活动标签页的 HTTPS origin，用于匹配当前网站；不读取页面正文，也不读取其他标签页。
- `scripting`：仅在用户点击“填充”后临时注入填充逻辑。
- `optional_host_permissions`：首次填充某个 HTTPS 网站时按站点请求页面访问权限，不预先访问所有网站。
