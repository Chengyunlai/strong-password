# Edge Add-ons 商店资料（草稿）

## 名称

Strong Password Vault

## 简短描述

使用密码学安全随机数生成强密码，并在本机加密保险库中安全保存和主动填充。

## 详细描述

Strong Password Vault 面向需要在不知名网站注册账号的用户：

- 默认生成 24 位高强度随机密码
- 支持按网站要求调整长度和符号
- 使用主密码保护本机加密保险库
- 提供一次性恢复密钥，不使用安全问题
- 仅在用户主动操作且完整 origin 匹配时填充凭据
- 支持加密备份导出与导入

数据不会上传到云端，扩展不提供自动同步服务。

## 隐私政策 URL

发布前请将 `PRIVACY.md` 放到一个公开 HTTPS 页面，并把 URL 填入商店后台。

## 权限说明

- `storage`：保存加密后的本机保险库记录。
- `activeTab` / `scripting`：响应用户主动点击，在当前页面填充匹配 origin 的凭据。
- `<all_urls>`：允许用户在任意网站使用主动填充功能；扩展不会自动提交表单。
